# things sway should know as a session from greetd
PATH="$PATH:$HOME/.local/bin"

export PATH
export REUSE_NOTIF_DIR="/run/user/$UID/persistent_notif_id"
export INHIBIT_DIR="/run/user/$UID/inhibit_sleep"
