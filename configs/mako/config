# === === STYLE === ===
font=JetBrainsMono 11
# colors
text-color=#FFFFFF
# notifs r a pale yellow and pink to grab attention
# (and it looks like ice cream)
background-color=#45475A
border-color=#A6E3A1
progress-color=over #ffbfbf
# other styling
anchor=top-center
border-radius=8
padding=7
width=450

# default timeout: 10 seconds
default-timeout=10000

[mode=dnd]
invisible=1
