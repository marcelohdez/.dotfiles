# === === STYLE === ===
font=CascadiaMono 11
# colors
text-color=#FFFFFF
# notifs r a pale yellow and pink to grab attention
# (and it looks like ice cream)
background-color=#151515
border-color=#5E5E5E
progress-color=over #3E3E3E
# other styling
anchor=top-center
border-radius=8
padding=7
width=450

max-history=10
# default timeout: 10 seconds
default-timeout=10000

[mode=dnd]
invisible=1
