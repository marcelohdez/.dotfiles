# === === STYLE === ===
font=CascadiaMono, SymbolsNerdFont 11
# colors
text-color={{colors.text_color_primary.default.hex}}
background-color={{colors.on_secondary.default.hex}}
border-color={{colors.secondary.default.hex}}
progress-color=over {{colors.secondary_container.default.hex}}

# other styling
anchor=top-center
border-radius=8
padding=7
width=450

on-button-left=dismiss
on-button-right=invoke-default-action
max-history=10
max-visible=2
# default timeout: 10 seconds
default-timeout=10000

[mode=dnd]
invisible=1
