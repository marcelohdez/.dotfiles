# === === STYLE === ===
font=CascadiaMono 11
# colors
text-color=@{text_color_primary}
background-color=@{on_secondary}
border-color=@{secondary}
progress-color=over @{secondary_container}

# other styling
anchor=top-center
border-radius=8
padding=7
width=450

max-history=10
max-visible=2
# default timeout: 10 seconds
default-timeout=10000

[mode=dnd]
invisible=1
